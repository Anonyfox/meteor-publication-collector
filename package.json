{
  "name": "anonyfox-publication-collector",
  "version": "0.0.1",
  "description": "Collect publications for testing in Meteor",
  "private": true,
  "scripts": {
    "test": "TEST_CLIENT=0 meteor test-packages ./ --once --driver-package meteortesting:mocha --raw-logs"
  },
  "devDependencies": {
    "@types/meteor": "^2.9.8",
    "@types/mocha": "^10.0.10",
    "@types/chai": "^5.0.1",
    "typescript": "^5.4.5"
  }
}

